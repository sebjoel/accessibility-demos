import React, { ReactNode } from "react";
import "./Header.scss";

type HeaderProps = {};

export default function Header({}: HeaderProps) {
  return <header className="demo-Header">Header</header>;
}
